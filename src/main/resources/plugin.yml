name: CustomDiscs
version: ${version}
main: na.Navoei.customdiscsplugin.CustomDiscs
api-version: ${bukkit_api_version}
prefix: CustomDiscs
authors: [ "Navoei" ]
description: A plugin which used the Simple Voice Chat API to add custom music discs.
depend: [ voicechat ]

commands:
  customdisc:
    description: Create a custom disc!
    aliases: cd